public interface Constants {
	public interface Modes {
		String SERVER = "server";
		String CLIENT = "client";
	}
	public interface Modules {
		String TCP = "tcp";
		String UDP = "udp";
		String MTCP = "mtcp";
		String SRPC = "srpc";
		String ARPC = "arpc";
	}
	public interface Ports {
		String LOCAL_PORT = "8080";
	}
}
